## 1. 协议与解析骨架

- [x] 1.1 为组级 AI 响应引入新的数据模型，覆盖 GroupUpdate、GroupEvent、CharacterUpdate、Intent、Action、Basis 及其子对象
- [x] 1.2 扩展 `src/ai/parser.py`，支持解析组级 `participants`、场景、事件和角色账本结构
- [x] 1.3 在解析层加入基础归一化与最小结构检查，确保必须字段缺失时能返回明确错误

## 2. 统一交互组处理路径

- [x] 2.1 调整 `AICoordinator` 和相关调用路径，使单角色更新与多角色更新统一走组级协议流程
- [x] 2.2 调整 `PromptBuilder`，为交互组提示词声明必须字段、`basis` 结构和白名单 `action_type` / `event.type`
- [x] 2.3 更新 AI 交互链路，确保组级场景摘要、场景描述和角色账本能够回传到主循环

## 3. 强审计与裁剪

- [x] 3.1 新增独立的 AI 审计/校验模块，并实现组边界、结构完整性和存在性审计
- [x] 3.2 实现库存守恒、法宝状态边界、HP/MP/神识边界和轻微超界裁剪规则
- [x] 3.3 实现保守失败策略：对白名单类型非法、共享事实冲突、库存不足、严重资源超支等硬错误整组失败
- [x] 3.4 输出结构化审计结果，至少包含错误、警告和裁剪后的值，供编排层和日志使用

## 4. 应用器与主循环重构

- [x] 4.1 收窄 `ChangeApplier` 职责，使其仅消费已通过审计的组级结果并应用角色账本
- [x] 4.2 调整 `GameLoop` 在组级更新通过后统一写入场景摘要/场景描述日志，在失败时记录审计错误
- [x] 4.3 保持 `InteractionDetector` 只负责分组，不引入审计或应用逻辑

## 5. 测试与回归验证

- [x] 5.1 为组级响应解析补测试，覆盖单角色组、多角色组和缺少关键字段场景
- [x] 5.2 为强审计补测试，覆盖组成员不一致、非法类型、库存不足、严重资源超支和共享事实冲突场景
- [x] 5.3 为安全裁剪补测试，覆盖 HP/MP/神识上下界、法宝耐久/损耗、轻微超上的注灵量场景
- [x] 5.4 为通过审计后的组级应用流程补测试，验证日志写入、角色账本落地和失败时整组不应用

